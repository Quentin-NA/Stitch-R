<div class="container text-left">
  <div class="pl-2">
    <h1>Historique</h1>
    <h3>filtres</h3>
  </div>
    <table class="table table-hover table-responsive">
      <thead>
        <tr>
          <th>Date/Heure</th>
          <th>Objet</th>
          <th>Statut</th>
        </tr>
      </thead>
      <tbody>
        <% @receipts.each do |receipt| %>
          <tr class = "font-weight-light">
            <td><%= receipt.send_date.strftime("%d-%m-%y à %H:%M") %> </td>
            <td><strong><%= receipt.subject %></strong>
            <td><strong><%= receipt.status %></strong>
          </tr>
        <% end %>
      </tbody>
    </table>
</div>

