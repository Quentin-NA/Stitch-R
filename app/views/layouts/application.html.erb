<!DOCTYPE html>
<html>
  <head>
    <title>Stitchr est une solution d'automatisation gratuite
           qui trie les messages de vos fournisseurs préférés
           et vous permet de les partagez comme vous le souhaitez.</title>
    <%= favicon_link_tag %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
    <%= render 'shared/navbar'%>
    <%= render 'shared/flashes' %>
    <%= render 'shared/sidebar' unless controller_name == "pages" && action_name == "home"%>

  <% if controller_name == "pages" && action_name == "home" %>
    <div>
      <div class="content-wrapper">
        <%= yield %>
      </div>
      <p class="alert"><%= alert %></p>
    </div>
    <%= render 'shared/footer' %>
    <% else %>
      <div class="with-dashboard-container pt-3">
        <%= yield %>
      </div>
  <% end  %>
  </body>
</html>
