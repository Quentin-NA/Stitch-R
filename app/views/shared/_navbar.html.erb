<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to supplier_searches_path, class: "navbar-brand" do %>
    <%= image_tag "logostitchr.png" %>
    <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active pr-2 pt-2">
          <p>Hello <%= current_user.first_name %> !</p>
        </li>
        <li class="nav-item dropdown">
          <% avatar_url = current_user.google_picture_url || "http://placehold.it/30x30" %>
          <%= image_tag avatar_url, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Mon profil", "profil", class: "dropdown-item" %>
            <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
        <%= link_to omniauth_authorize_path(:user, :google_oauth2), id: "login-link" do %>
          <p id="login">Login</p>
        <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
